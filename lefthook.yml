pre-commit:
  commands:
    lint:
      glob: "**/*.{js,cjs,mjs,ts,tsx,json,jsonc,md,yml,yaml}"
      run: bunx biome check --no-errors-on-unmatched {staged_files}

commit-msg:
  commands:
    conventional:
      run: bunx commitlint --edit {1}
